{
  "name": "ksensetch-assessment",
  "version": "1.0.0",
  "description": "DemoMed Healthcare API assessment solution",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "fetch": "ts-node src/index.ts --fetch",
    "submit": "ts-node src/index.ts --fetch --submit",
    "test": "npm run build"
  },
  "dependencies": {
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^18.16.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.0"
  }
}
